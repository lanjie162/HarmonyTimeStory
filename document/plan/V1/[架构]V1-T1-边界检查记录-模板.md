# 技术方案评审决策纪要

**会议类型**：技术方案评审（`meeting-facilitator` 规则：路径 `document/techdoc/`）  
**评审对象**：[时光故事-MVP技术方案-v1.md](../techdoc/时光故事-MVP技术方案-v1.md) **V1.4.0**  
**日期**：2026-05-06  
**参会子代理（发言顺序）**：`architect` → `dev` → `qa`  
**关联参考**：MVP [时光故事-MVP需求说明-v4.md](../requirement/时光故事-MVP需求说明-v4.md) **V4.4**；UI 原型 [时光故事-MVP-UI原型-v1.md](../design/时光故事-MVP-UI原型-v1.md) **v1.2**

---

## 【自动推导】

**评审类型**：技术方案评审；**本轮子代理**：architect、dev、qa；**依据**：`document/techdoc/时光故事-MVP技术方案-v1.md`（V1.4.0）、目录 `document/techdoc/` 及用户「技术方案评审」意图。

---

## 1. 分模块评审记录

### M1 · §2 基线与约束

| 角色 | 要点 |
|------|------|
| 架构师 | API12、单 entry、不落盘原图、端侧人脸与调研表衔接清晰；真机矩阵以 TODO §C 驱动回填再并入 §10，需在计划中预留「矩阵未定→阈值待定」缓冲。 |
| 研发 | 与工程约束可对齐；注意 `build-profile.json5` 与文档一致。降级矩阵若滞后，实现预留配置桩（IX-02 分档），避免硬编码锁死。 |
| 测试 | 验收基线依赖 API12 与真机矩阵；矩阵未回填前，性能/降级类用例标为条件执行；建议 §C 关闭后再定完整回归集。 |

**模块小结**：基线冻结口径认可。需产品/测试明确矩阵未齐时的提测范围。

---

### M2 · §3 总体架构（分层、`api/`、`pages` 映射、硬约束、Mock）

| 角色 | 要点 |
|------|------|
| 架构师 | 四层 + 双 `api/` 清晰；`IImportService` / `ISuggestionService` 分离利于形态三与导入向导解耦；§3.3.1 与 UI v1.2 对齐。`bootstrap` 装配复杂度随接口上升，首 PR 需定注入拓扑与生命周期。 |
| 研发 | `pages` 禁碰 infrastructure 利于 UT；路由域多，可先占位页 + 导航骨架；优先「人物列表→详情→占位导入」再扩展。 |
| 测试 | §3.6 Mock 分层替身利于契约测试；验收时核对 Stub 能否覆盖 §11 追溯，避免只测壳不测编排。 |

**模块小结**：架构与路由映射可收口。建议实现 PR 附 bootstrap 与路由注册 checklist。

---

### M3 · §4 数据模型与持久化

| 角色 | 要点 |
|------|------|
| 架构师 | `PhotoOwnerLink` + `PhotoRef` 生命周期与 MVP §6 对齐；GC 与封面计数同事务正确。O-6 `scanSummaryJson` schema 待扫描首版锁定。 |
| 研发 | §4 未详写 RDB 迁移与版本号策略，实现时需补；头像沙箱与 `PhotoRef` 分离已写清。 |
| 测试 | 高价值用例：ST-07 第 1000/1001 张、同 URI 复用、边删完→SOFT_DELETED→GC、人物删级联；宜表驱动 + 事务断言。 |

**模块小结**：数据层可作为文档基线。补充 RDB 迁移策略（§4 或 §12）。O-6 建议在扫描首版 PR 由研发 + 架构关闭。

---

### M4 · §5 媒体与 PhotoPicker

| 角色 | 要点 |
|------|------|
| 架构师 | Picker 非阻塞；IM-09 / R-05 与 `cancelRequest` 与 B2 一致；ER-01 依赖 TODO §C3 真机结论。 |
| 研发 | 云相册/本地删除分支多，错误码→UI 文案宜集中映射。 |
| 测试 | 弱网/云端不可用/权限撤销独立场景；取消需覆盖系统 cancel + 应用协作标志（R-05）。 |

**模块小结**：主线认可；§C3 回填前 ER-01 文案按草案验收。

---

### M5 · §6 相册导入与智能关联（三形态、IM-09 UI）

| 角色 | 要点 |
|------|------|
| 架构师 | 形态一/二含导入准备；形态三 `ISuggestionService` + 顶栏徽章→suggest 已与 MVP/UI 对齐；§6.3 要求长任务 UI「全屏 vs BottomSheet」工程内二选一一致。O-1/O-2 仍影响性能上限叙事。 |
| 研发 | 导入链路状态机（准备/扫描/候选）建议单模块；形态三与 IM-10 双选项、队列清空顺序需理清并发。 |
| 测试 | 三形态各至少一条主路径 E2E；取消贯穿扫描；IM-10 组合（关/开 × 清空/不清）；人脸筛选依赖 READY 须有降级或等待策略用例。 |

**模块小结**：三形态技术落点通过。首个可运行版本须**锁定长任务 UI 形态**并写入工程说明或 §13。待决策：O-2（全库 vs 时间窗）验证后再写入 §6；形态三「已入库」扫描边界首版锁定文案。

---

### M6 · §7～§10、§11 追溯、§12 开放项

| 角色 | 要点 |
|------|------|
| 架构师 | §7 PR-02、§9 崩溃边界、§10 权限、§8 主题横切闭环；§11 已补 UI 路由域；§12 O-5 与 UI 定稿挂钩，关闭条件可执行。 |
| 研发 | O-3 崩溃 SDK、O-4 tagsJson 属集成阶段，不阻塞架构评审但进发版前 checklist。 |
| 测试 | §11 用于双向追溯；提测建议导出矩阵勾选表；开放项对应专项测试（崩溃隐私、权限审核）。 |

**模块小结**：追溯与开放项管理清晰。O-3 SDK 选型建议在首个内测构建前落定。

---

## 2. 评审结论

**有条件通过**：以 §12 开放项、真机矩阵回填、长任务 UI 形态锁定为实现阶段门禁；**不阻塞**以当前文稿 **V1.4.0** 作为开发基线。

---

## 3. 决策纪要表

| 分类 | 内容 |
|------|------|
| **✅ 已确认项** | V1.4.0 四层架构与 `api/` 边界；`IImportService` / `ISuggestionService` 分离；§3.3.1 与 UI v1.2 路由域映射；§4 `PhotoOwnerLink` + `PhotoRef` 主路径；§6 三形态与导入准备、形态三入口表述；§6.3 长任务取消与 UI 一致性原则；§11 追溯扩展；§12 O-5 关闭语义更新。 |
| **⚠️ 需修改项** | 补充 **RDB 迁移/版本** 说明（§4 或 §12）；首个实现 PR **锁定长任务 UI 形态**（全屏 vs BottomSheet）并文档化；**bootstrap 装配与路由** checklist；ER-01 在 §C3 回填后修订文案对齐。 |
| **❌ 暂缓项** | 数值化降级矩阵全文并入 §10（待 TODO §C）；**O-6** `scanSummaryJson` schema 细节（待扫描首版）。 |
| **📅 后续行动项** | **dev**：首路径 PR（路由壳 + 状态机骨架 + RDB v1）；**architect**：扫描首版 PR 评审 **O-6** 与 **O-2** 结论回填；**qa**：基于 §11 建追溯表草案，§C 完成后补全降级/性能用例；**project-manager**：把 §C 与 O-3 时间节点纳入迭代（见 [TODO-跟踪.md](../TODO-跟踪.md)）。 |

---

## 4. 待决策清单（可选下一轮收口）

1. **O-2**：相册扫描数据源（全库 vs 时间窗预剪枝）— 性能验证后再写入 §6。  
2. **长任务 UI**：全屏进度页 vs 后台 + BottomSheet — 工程内锁定一种并与 UI §1 一致。  
3. **形态三扫描范围**：§6.2「已入库」边界 — 首版实现锁定并记入 §13。  
4. **O-3**：崩溃 SDK 选型与初始化 — 建议首个内测构建前关闭。

---

## 附：会议程序说明（归档）

本场纪要对应会话为 **批量汇总产出**：执行时 **未** 严格按 `meeting-facilitator` **默认交互式节奏**（逐模块输出后暂停、等待用户回复「继续」再进入下一模块）。纪要中的技术结论与行动项仍可作为评审输入；若需与「交互式会议」程序完全等价，可对各模块结论 **逐项书面确认** 或 **按模块重开评审回合**。现行主持人规则见项目 [.cursor/agents/meeting-facilitator.md](../../.cursor/agents/meeting-facilitator.md) **「阶段二（附）：会议节奏」**。

**更新（2026-05-06）**：已另完成 **交互式逐模块技术方案评审**，用户裁定见 [2026-05-06-技术方案评审-交互决策附录.md](./2026-05-06-技术方案评审-交互决策附录.md)（**裁定以该附录为准**；与本批量纪要不一致处，以附录覆盖）。

---

**文档结束**
