---
task_request: TR-20260509-02#C-5(closed)
plan_anchor: document/plan/V2/[项目经理]2026-05-08-MVP-V2-最终工作计划.md#V2-T5-可执行验收口径
ui_baseline: document/design/时光故事-MVP-UI原型-v1.md v1.2（§2.2 / §4.5 / §4.6 / §4.0）
owner_role: ued
executed_at: 2026-05-09
---

# V2-T8 · 导入链路交互对版 · 差异清单（UED 主笔）

## 结论（总判定）

对照计划 **`§V2-T5 可执行验收口径`** 七条与当前实现（`ImportPage.ets`、`SuggestPage.ets`、人物/故事详情入参路由），存在 **若干条关键与 v1.2 不一致**，已在下文逐条显式登记；与 **`V2-T3`/`V2-T4`** 已交付的工程骨架偏差（P-2 全屏进度骨架、应用层 `cancelRequest`）**不与「未记载冲突」**——已在 `document/evidence/V2/2026-05-09-T6-import-wizard-minimal.md`、T-7 证据及本清单 **§与 dev 收敛建议** 交叉引用。  
**是否满足计划「总判定」字面**（无关键错配 **或** 可接受显式降级）：当前结论为 **「关键差异已列全，可进入并行裁决 / 后续 dev 收敛轮」**；**不建议**在未收敛前由验收方勾选「无关键错配」为通过（除非 pm/qa 会签为 **条件通过** 并附条件清单）。

---

## §V2-T5 七条 · 逐项走查

| # | 核对点（摘自计划表） | 实现现状（代码/入口） | 差异等级 | 说明与建议 |
|---|----------------------|------------------------|----------|------------|
| 1 | 步序与形态一/二入口；面包屑示目标标题/显示名 | 步序骨架一致：`pick→prepare→conditions→scan→confirm`；详情 `router.pushUrl` 传 `ctx`+`ownerId` | **非关键** | 面包屑为「形态一/二 · #id」，**未**展示故事标题/人物显示名；建议 dev 传入或详情页二次查询补全。 |
| 2 | 导入准备 A.5：取消返回上一步不写业务边；完成后自动进条件 | 准备步有「上一步（重选）」清空 URI；「下一步：条件」**手动** | **非关键～中** | 无单独「取消」文案（与「上一步」合并语义）；**非自动**进入条件，与 §4.5 A.5「完成后自动」有差异；可降级为「显式下一步」并改文案对齐。 |
| 3 | 条件页：时间/地点/人脸三块与 IM-04～07、IM-10 | 仅 HEIC、重复 URI 两枚 Toggle 骨架 | **关键** | 与 v1.2 **§4.5 步骤 B** 结构不一致；需 dev+ued 一轮补全或走 **并行冲突裁决** 登记「V2 仅骨架」范围。 |
| 4 | 扫描 IM-09：对话框确认取消 →「正在取消…」禁用 → 回条件保留条件 | 全屏进度 + `取消扫描` 即 `cancelRequest`，无二次确认、无「正在取消…」独立态 | **关键（已预登记）** | 与 T-6/T-7 证据及计划「P-2 骨架」一致；若维持应用层 B2，建议在文案与禁用节奏上 **逼近** v1.2，或 CR 收窄 V2 口径。 |
| 5 | 候选 IM-08：网格勾选、全选/反选、「确认导入」、ST-07 阻断与回详情 | 文本列表预览 URI；主按钮为「**确认写入**」；ST-07 主要在选图端 ≤9 | **关键** | 缺勾选/全选反选；按钮命名与 §4.5 D 不一致；候选步 ST-07 二次阻断未体现。 |
| 6 | 形态三：仅人物 Tab **顶栏徽章** 进 `suggest`，不与导入共链路 | `PersonDetailPage`、`StoryDetailPage` 均可 `pushUrl` 至 `SuggestPage`；**人物列表顶栏无徽章** | **关键** | 违反 §2.2 / §4.0 图 5 入口约束与「仅人物 Tab 顶栏」；故事侧进建议为 **入口级错配**，建议 dev 移除故事入口 + Shell/Person 顶栏补徽章（或走 CR 改基线）。 |
| 7 | 形态三 §4.6：顶部说明与接受/忽略/批量 | `SuggestPage` 仍为壳页（空/加载/错误切换），无 §4.6 主文案与列表动作 | **关键** | 与 v1.2 形态三最小可测内容未对齐；需 ued 出文案块 + dev 落地列表与批量语义。 |

---

## DoD-2 ·「建议审阅」与「导入向导」入口混用

| 检查项 | 结论 |
|--------|------|
| 同一按钮文案把导入与建议混为一谈 | **未发现**：详情分别为「进入导入向导壳页」与「进入建议页壳页」，路由 `ImportPage` / `SuggestPage` 分离。 |
| 形态三与导入「从选图起步」链路混用 | **未发现**：建议页不启动相册选图导入链。 |
| 形态三 **唯一合规入口**（人物 Tab 顶栏徽章） | **未满足**：见上表 **#6**。 |

---

## DoD-3 · 与 dev 取消/进度语义冲突（未裁决）

| 材料 | 结论 |
|------|------|
| `ImportFeature.ets` / `ImportPage.ets` 与 T-7 证据 | 应用层 `cancelRequest` + 扫描轮询，与计划 **B2 原生** 表述有 **已知实现边界** |
| 计划 **§并行冲突裁决规则** | 本清单将 #3/#4/#5/#6/#7 列为 **ued 基线侧变更点**；未闭合前验收应 **条件通过** 或阻塞升级 **manager** |

---

## 附录 · 抽检代码锚点（便于 pm/qa 会签）

- `entry/src/main/ets/pages/import/ImportPage.ets` — 导入主链与全屏扫描
- `entry/src/main/ets/pages/suggest/SuggestPage.ets` — 建议壳页
- `entry/src/main/ets/pages/person/PersonDetailPage.ets` — 导入 / 建议 `pushUrl`
- `entry/src/main/ets/pages/story/StoryDetailPage.ets` — 同上（建议入口）
- `entry/src/main/ets/pages/person/PersonPage.ets` — 当前无形态三顶栏徽章

---

## 与 dev 协作的下一步（非本任务强制闭环）

1. P0：修正形态三入口（#6）与候选/条件/取消交互（#3～#5、#4 文案与节奏）。  
2. P1：面包屑展示目标名（#1）、准备→条件自动/文案（#2）。  
3. 形态三 §4.6 全页（#7）单独立项或与 V3 暂缓策略对齐（见计划 **暂缓项 B** 若适用）。
