# V1.0 TDD 测试能力落地 — 八任务派发

> 上游依据：[QA]测试能力升级方案_分层TDD架构.md、2026-05-30-TDD架构方案评审决策纪要.md、[项目经理]TDD测试能力落地_最终工作计划.md
> 梯度：V1.0（P0 · 最高优先级）
> 派发日期：2026-05-30

---

## 任务清单

### C-1 T-51：TestDataFactory 实现

- **owner_role**：dev
- **DoD**：
  - 可调用 createPerson、createStory、createPhoto 工厂方法
  - 支持参数化造数（可选参数提供默认值）
  - 参照现有 Tier1RdbIntegration.test.ets 的做法风格
  - 代码位置：`entry/src/ohosTest/ets/test/utils/TestDataFactory.ets`
- **证据**：L1（代码提交）

---

### C-2 T-52：TestDbHelper 实现（时间戳独立库）

- **owner_role**：dev
- **DoD**：
  - 每次创建独立数据库文件（文件名含时间戳/随机后缀）
  - 支持创建绑定 Schema + 清理删除数据库文件
  - 与 ServiceTestBase 配合，setup 创建、teardown 销毁
  - 直接参照现有 OhosTestRdbContext.ets
- **证据**：L2（真机执行测试通过日志）

---

### C-3 T-53：ServiceTestBase 基类实现

- **owner_role**：dev
- **DoD**：
  - setup：创建独立数据库 → 构造 RepositoryImpl → 构造 Service 实例
  - teardown：销毁 Service → 关闭数据库 → 删除文件
  - 子类仅需实现 `createService()` 方法
- **依赖**：T-51 + T-52
- **证据**：L1（代码提交）

---

### C-4 T-54：PersonService L3 集成测试

- **owner_role**：dev
- **acceptor_role[]**：qa
- **DoD**：
  - 所有公开方法有测试覆盖（~10 方法，~25 条用例）
  - 包括近期 bug 回归：updatePerson 空参数返回 true、6 个 bug 回归
  - 命名规范：`L3_personService_${方法}_${场景}`
- **依赖**：T-53
- **证据**：L2（真机 hdc aa test 全通过日志）

---

### C-5 T-55：StoryService L3 集成测试

- **owner_role**：dev
- **acceptor_role[]**：qa
- **DoD**：
  - 所有公开方法有测试覆盖（~10 方法，~25 条用例）
  - 包括近期 bug 回归：updateStoryAll 部分字段全部生效
  - 命名规范：`L3_storyService_${方法}_${场景}`
- **依赖**：T-53
- **证据**：L2（真机 hdc aa test 全通过日志）

---

### C-6 T-56：LinkService + PhotoService L3 集成测试

- **owner_role**：dev
- **acceptor_role[]**：qa
- **DoD**：
  - 关键方法有测试覆盖（~14 方法，~35 条用例）
  - 包括完整生命周期（link→isLinked→unlink）、带 metadata 的 addPhoto、story cap=1000 上限
- **依赖**：T-53
- **证据**：L2（真机 hdc aa test 全通过日志）

---

### C-7 T-57：近期 6 个 bug 回归用例

- **owner_role**：dev
- **acceptor_role[]**：qa
- **DoD**：
  - 6 个已知 bug 的测试用例已加入对应测试文件（PersonService/StoryService 等）
  - 每个 bug 至少一条 FAIL → PASS 用例
- **依赖**：T-54 + T-55 + T-56
- **证据**：L1（代码提交 + test pass 日志）

---

### C-8 T-58：project_rules.md 规范文档补充

- **owner_role**：manager
- **DoD**：
  - 分层 TDD 规范已写入 project_rules.md
  - 包含测试分层定义、TDD 流程、命名规范、Code Review 检查清单
- **证据**：L1（project_rules.md diff）