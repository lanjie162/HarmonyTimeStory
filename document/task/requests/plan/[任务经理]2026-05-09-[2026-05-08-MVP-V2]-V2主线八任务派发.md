---
request_id: TR-20260509-02
source_type: plan
source_doc: document/plan/V2/[项目经理]2026-05-08-MVP-V2-最终工作计划.md
source_anchor: §计划任务清单（V2）
created_at: 2026-05-09
created_by: manager
status: accepted
scope: 自 V2 定稿计划派发 V2-T1～V2-T8 执行包，对齐 V2-D1～D8、关键路径与并行轨前置依赖
item_count: 8
notes: 2026-05-09 全量接收；明细 C-1～C-8 对应统一任务列表 T-4～T-11。**2026-05-10 通晒**：C-1～C-5 已 closed 并归档；C-6～C-8 仍对应 T-9～T-11（待执行），明细未 closed。
---

## §派发概要

本次派发来源于 **`document/plan/V2/[项目经理]2026-05-08-MVP-V2-最终工作计划.md`** 之 **§计划任务清单（V2）** 与 **可选·打包推荐** 中已定稿条目，将 **规划视角**转写为可执行工单（`owner` / `acceptor` / 可验证 `DoD` / 文件内 **`C-<n>` 依赖闭包**）。**`B2+C9`同版闭环**（`P-3`）、**交互全屏进度**（`P-2`）与 **§V2-T5 可执行验收口径** 作为跨任务约束，执行中不得未经 `CR-x` 扩大 `P-1` 边界。

上游编号与本文件明细映射：

| 计划任务编号 | 本请求明细编号 |
|--------------|----------------|
| V2-T1 | C-1 |
| V2-T2 | C-2 |
| V2-T3 | C-3 |
| V2-T4 | C-4 |
| V2-T5 | C-5 |
| V2-T6 | C-6 |
| V2-T7 | C-7 |
| V2-T8 | C-8 |

## §任务明细

| 明细编号 | 任务摘要 | owner_role | acceptor_role[] | DoD | 复杂度 | 优先级 | 依赖 | 来源回链 |
|----------|----------|------------|-----------------|-----|--------|--------|------|----------|
| C-1 | 人物主流程实现与回归（列表/详情/编辑/关联故事入口/手动加图入口） | dev | [qa] | 1）人物 Tab 列表→详情→编辑主路径单次会话内可走通且无阻塞性崩溃；2）详情内「关联故事」入口与手动加图入口可被验收方复现并成功落库/`PhotoOwnerLink` 主路径可追溯；3）关键状态可见（空态、加载、写入结果反馈至少有其一可截图或日志佐证）并对照需求 `PE-*`/`PH-01` 无显性违背 | M | P1 | - | `document/plan/V2/[项目经理]2026-05-08-MVP-V2-最终工作计划.md` §计划任务清单（V2）·行 V2-T1 |
| C-2 | 故事主流程实现与回归（列表/详情/编辑/关联人物入口/手动加图入口） | dev | [qa] | 1）故事 Tab 列表→详情→编辑主路径单次会话可走通且无阻塞性崩溃；2）「关联人物」与手动加图入口可复现并成功挂接；3）关键状态可见并对照需求 `ST-*`/`PH-01`/`ST-07` 显性规则无违背 | M | P1 | - | 同上 · 行 V2-T2 |
| C-3 | 导入主链骨架落地（形态一/二：导入准备、条件、扫描、候选确认） | dev | [qa] | 1）形态一（故事上下文）与形态二（人物上下文）均能进入并完成「选图→导入准备→条件→扫描→候选确认→确认写入」主顺序；2）中途状态切换与 **`IM-09` 取消/重试** 与 **`UI v1.2`** **§4.5**、`P-2` 全屏进度形态不出现逻辑冲突（若实现与稿冲突须走并行裁决条文）；3）写入后详情页照片网格刷新可复现，`ST-07`/`PH-01` 在去重或上限处有明确用户反馈 | L | P1 | C-1,C-2 | 同上 · 行 V2-T3 |
| C-4 | `B2`取消语义实现（`cancelRequest`+应用层兜底+取消中态与重试） | dev | [qa] | 1）扫描/导入长任务链路可触发取消，接口层 `cancelRequest` 与合作取消路径可复核；2）取消中「禁用/恢复」与失败后重试与用户可见文案与 **`§V2-T5`** / `IM-09` 对齐或已登记裁决偏差；3）关键日志或最小复现步骤可归档至 **`document/evidence/V2/`**（建议子目录对齐任务），满足后续 `V2-D4` 证据范式 | L | P1 | C-3 | 同上 · 行 V2-T4 |
| C-5 | V2交互一致性对版（导入链路文案、状态、入口一致） | ued | [pm, qa] | 1）按计划 **§V2-T5 可执行验收口径** 七条逐项走查并完成差异清单（无关键错配或可接受的显式降级已登记）；2）导入/形态三关键入口不把「建议审阅」与「导入向导」混用入口；3）与 `dev` 已实现的取消与进度语义无未裁决冲突（有则已进入 **并行冲突裁决规则** 并留有纪要或计划脚注） | M | P1 | C-3,C-4 | 同上 · 行 V2-T5 |
| C-6 | V2测试设计与执行（主链、取消链路、异常态最小集） | qa | [pm] | 1）输出可执行测试范围与计划，并 **`V2-D1～V2-D4`**（人物/故事/导入/`B2`）均有对应用例命中与一轮执行记录；2）阻塞/严重缺陷单列状态与闭环或升级结论；3）可追溯证据可按 **`document/plan/V2/qa/[QA]2026-05-09-V2关键验收证据最小模板.md`** 最小字段组织（路径/日志/判定） | M | P1 | C-1,C-2,C-3,C-4 | 同上 · 行 V2-T6 |
| C-7 | `C9` API12真机验证与证据归档（取消语义） | qa | [pm] | 1）API12 真机完成 **`R-05`**/`C9` 方案要求的最小场景，结论为通过或有条件通过并列明条件时限；2）取证（`hilog`/等价输出、构建号、机型）归入 **`document/evidence/V2/…`**，满足 **`V2-D5`** 与模板主档；3）与同版 **`B2`**结合场景已覆盖或明示缺口与风险（不得以拆版绕过 `P-3`） | M | P1 | C-4,C-6 | 同上 · 行 V2-T7 |
| C-8 | V2版本结项与V3输入边界归档（含风险与暂缓项） | pm | [qa] | 1）《结项说明》补齐 **`V2-D7`/`V2-D8`**：**未闭项**、风险责任、暂缓项与本版本 **`V2`** 对齐；2）`**V3` 准入输入**边界写清且无与 `P-1` 隐含冲突（若扩范围须 **`CR-x`**）；3）`pm`/`qa` 会签可追溯，关键结论有路径锚点指向本仓库文档 | S | P1 | C-5,C-7 | 同上 · 行 V2-T8 |

## §接收记录

| 明细编号 | 决策 | 统一任务列表编号 | accepted_at | closed_at | 完成证据链接 |
|----------|------|------------------|-------------|-----------|--------------|
| C-1 | accepted | T-4 | 2026-05-09 | 2026-05-10 | `document/task/archive/2026-05-10/[任务经理]T-4-人物主流程实现与回归.md` |
| C-2 | accepted | T-5 | 2026-05-09 | 2026-05-10 | `document/task/archive/2026-05-10/[任务经理]T-5-故事主流程实现与回归.md` |
| C-3 | accepted | T-6 | 2026-05-09 | 2026-05-10 | `document/task/archive/2026-05-10/[任务经理]T-6-导入主链骨架落地.md` |
| C-4 | accepted | T-7 | 2026-05-09 | 2026-05-10 | `document/task/archive/2026-05-10/[任务经理]T-7-B2取消语义实现.md` |
| C-5 | accepted | T-8 | 2026-05-09 | 2026-05-10 | `document/task/archive/2026-05-10/[任务经理]T-8-V2交互一致性对版.md` |
| C-6 | accepted | T-9 | 2026-05-09 |  |  |
| C-7 | accepted | T-10 | 2026-05-09 |  |  |
| C-8 | accepted | T-11 | 2026-05-09 |  |  |
