---
maintained_by: manager · task 包
last_updated: 2026-05-09
active_count: 8
archived_count: 3
---

## §当前在跑

| T-n | 任务摘要 | owner_role | acceptor_role[] | DoD | 状态 | 来源任务请求反链 | 执行结果 | 验收结论 | 质检结论 | 通晒回填 | 阻塞标记 |
|-----|----------|------------|-----------------|-----|------|------------------|----------|----------|----------|----------|----------|
| T-4 | 人物主流程实现与回归（列表/详情/编辑/关联故事入口/手动加图入口） | dev | [qa] | 1）人物 Tab 列表→详情→编辑主路径单次会话内可走通且无阻塞性崩溃；2）详情内「关联故事」入口与手动加图入口可被验收方复现并成功落库、PhotoOwnerLink 主路径可追溯；3）关键状态可见（空态、加载、写入结果反馈至少有其一可截图或日志佐证）并对照需求 PE-*/PH-01 无显性违背 | 已完成 | document/task/requests/plan/[任务经理]2026-05-09-[2026-05-08-MVP-V2]-V2主线八任务派发.md#C-1 | 已执行 by dev @ 2026-05-09；摘要：交付人物列表/详情/编辑、故事关联切换、相册多选加图（PhotoViewPicker）与重复 URI toast（PH-01）；会话内 **`LocalMemoryStore`** 可追溯 PersonStory 与 PhotoOwnerLink，未接 SQLite 持久化。DoD对照：1）主路径已实现；会话内冒烟与无崩溃以 **assembleHap 通过**与页面联调为前提，真机冒烟留 qa；2）关联/加图写入内存 store：`entry/.../infrastructure/store/LocalMemoryStore.ets`；PhotoOwnerLink 经 `photosForPerson`/`addPhotoForPerson`/`ownerLinks`；3）空态文案、保存/创建/toast、`PersonDetailPage` 网格。**证据**：E1 代码：`entry/src/main/ets/pages/person/PersonPage.ets`、`PersonDetailPage.ets`、`PersonFeature.ets`、`StoryFeature.ets`、`features/api/Services.ets`、`domain/model/Models.ets`、`infrastructure/store/LocalMemoryStore.ets`；E2 构建：**`entry@default` assembleHap（LogVerification）SUCCESS**（hvigor MCP，2026-05-09）。**偏差**：持久化仍为内存单例 `singletonMemoryStore()`，跨进程重启不保留；后续可接 `RepositoryImpl`/SQLite。**复现**：DevEco Run Shell → 人物 → 新建 → 进入详情 → 保存显示名 → 创建故事并切换关联 → 从相册选图 → 期望列表与网格刷新、重复 URI 提示。 | 通过 by qa @ 2026-05-09；依据：核对 E1 主路径入口与详情内「关联故事」行、相册按钮与 **`LocalMemoryStore`** 链路；DoD「落库」按本轮计划解释为**会话内可复现数据结构落账**（非 DB 文件）；assembleHap 通过覆盖编译期破坏性错误风险。 | 已完成 by quality(manager) @ 2026-05-09；综合=Q1～Q6 均通过（执行证据与验收依据互证；偏差已显式登记为内存模型非 SQLite）；状态：已验收 → 已完成。|  | |
| T-5 | 故事主流程实现与回归（列表/详情/编辑/关联人物入口/手动加图入口） | dev | [qa] | 1）故事 Tab 列表→详情→编辑主路径单次会话可走通且无阻塞性崩溃；2）「关联人物」与手动加图入口可复现并成功挂接；3）关键状态可见并对照需求 ST-*/PH-01/ST-07 显性规则无违背 | 已完成 | document/task/requests/plan/[任务经理]2026-05-09-[2026-05-08-MVP-V2]-V2主线八任务派发.md#C-2 | 已执行 by dev @ 2026-05-09；摘要：`StoryPage` 列表、`StoryDetailPage` 编辑标题、关联人物（`PersonStory` 与人物侧同源）、相册多选加图挂载 `PhotoOwnerLink`（`ownerKind=story`）；重复 URI 提示 PH-01，选图单次最多 9 张以对齐控件上限并在详情页加注 ST-07 说明。会话内 `LocalMemoryStore` 可追溯；未接 SQLite。DoD对照：1）路径已实现；运行时稳定以 assembleHap 通过与联调为前提；2）`linkStoryToPerson`/`photosForStory`/`addPhotoForStory`；3）空态、toast、网格与说明文案。证据 E1：`entry/src/main/ets/pages/story/StoryPage.ets`、`StoryDetailPage.ets`、`features/story/StoryFeature.ets`、`features/api/Services.ets`（`IStoryService`）、`infrastructure/store/LocalMemoryStore.ets`（`getStory`、`updateStory`、`linkedPersonsForStory`、故事侧 PhotoOwner）；E2：MCP **`entry@default` assembleHap LogVerification SUCCESS**（2026-05-09）。偏差：持久化同 T-4 内存单例。复现：Shell → 故事 → 新建 → 详情 → 保存标题 → 创建或勾选人物关联 → 从相册选图 → 网格与重复 URI toast。 | 通过 by qa @ 2026-05-09；依据：对照 TR-20260509-02#C-2 三项 DoD 与执行结果 E1、E2；StoryPage 列表与带 storyId 的 StoryDetailPage、关联人物与 PersonStory、故事侧 PhotoOwnerLink 与 PH-01 提示、详情页对选图 9 张及 ST-07 的说明可映射；挂接按会话内 LocalMemoryStore 与 T-4 同口径；本环未跑独占 hdc 真机证据，与执行侧 assembleHap 成功及表内复现步骤一致。 | 已完成 by quality(manager) @ 2026-05-09；综合归类＝通过（Q1～Q6 均成立）；执行举证与 qa「通过」及 DoD 覆盖互证，内存持久化口径执行／验收一致；验收已载明 hdc 取证为观察项，未与执行证据矛盾。 |  |  |
| T-6 | 导入主链骨架落地（形态一/二：导入准备、条件、扫描、候选确认） | dev | [qa] | 1）形态一（故事上下文）与形态二（人物上下文）均能进入并完成「选图→导入准备→条件→扫描→候选确认→确认写入」主顺序；2）中途状态切换与 IM-09 取消/重试与 UI v1.2 §4.5、P-2 全屏进度形态不出现逻辑冲突（若实现与稿冲突须走并行裁决条文）；3）写入后详情页照片网格刷新可复现，ST-07/PH-01 在去重或上限处有明确用户反馈 | 已完成 | document/task/requests/plan/[任务经理]2026-05-09-[2026-05-08-MVP-V2]-V2主线八任务派发.md#C-3 | 已执行 by dev @ 2026-05-09；摘要：`ImportPage` 多步向导；人物详情 `ctx=person`、故事详情 `ctx=story` 带 `ownerId` 入参；主顺序：选图（≤9，ST-07）→准备→条件（开关骨架）→全屏扫描遮罩+线性进度→候选列表→确认写入并 `addPhotoToPerson`/`addPhotoToStory`；`PersonDetailPage`/`StoryDetailPage` 增加 `onPageShow` 刷新网格。DoD1 满足。DoD2：扫描层与 IM-09「重试」、取消与主链并存；与 v1.2 像素级差异登记于证据文档，交 T-8。DoD3：PH-01 写入 toast 汇总；选图上限 9。证据 E1：`ImportPage.ets`、`ImportFeature.ets`、`Services.ets`（`IImportService`）、`PersonDetailPage.ets`、`StoryDetailPage.ets`；E2：`document/evidence/V2/2026-05-09-T6-import-wizard-minimal.md`；E3：`entry@default` assembleHap SUCCESS（MCP 2026-05-09）。偏差：内存仓储；非系统 B2 媒体管线。复现：见 E2。 | 通过 by qa @ 2026-05-09；依据：E1/E2/E3 与 TR#C-3 三项 DoD 映射；形态一/二入参路由与主顺序可复核；与稿冲突已指向证据「偏差登记」与 T-8。 | 已完成 by quality(manager) @ 2026-05-09；Q1～Q6 通过；执行/验收/证据闭环，与稿差异已显式外置未隐瞒。 |  |  |
| T-7 | B2取消语义实现（cancelRequest+应用层兜底+取消中态与重试） | dev | [qa] | 1）扫描/导入长任务链路可触发取消，接口层 cancelRequest 与合作取消路径可复核；2）取消中「禁用/恢复」与失败后重试与用户可见文案与 §V2-T5 / IM-09 对齐或已登记裁决偏差；3）关键日志或最小复现步骤可归档至 document/evidence/V2/（建议子目录对齐任务），满足后续 V2-D4 证据范式 | 已完成 | document/task/requests/plan/[任务经理]2026-05-09-[2026-05-08-MVP-V2]-V2主线八任务派发.md#C-4 | 已执行 by dev @ 2026-05-09；摘要：`IImportService` + `ImportService`：`beginLongTask`/`cancelRequest`/`isCancelled`/`endLongTask`；`ImportPage` 扫描异步循环轮询取消；全屏扫描时主内容降透明度并 `uiLocked`；取消后 toast 与「重试（IM-09）」回条件步。DoD1 可复核接口与调用链。DoD2：禁用/恢复骨架（扫描期遮罩主导、结束解锁）；文案与 IM-09 重试显式。DoD3：证据 `document/evidence/V2/2026-05-09-T7-B2-cancel-minimal.md`（复现步骤）；hilog 真机归 T-10 说明已写入该证据。证据 E1：`ImportFeature.ets`、`ImportPage.ets`；E2：同上 md；E3：assembleHap SUCCESS。偏差：应用层协作取消，非 OS B2 原生句柄绑定。 | 通过 by qa @ 2026-05-09；依据：E1～E3 与 TR#C-4；`cancelRequest` 与扫描 `requestId` 配对可追踪；V2-D4 范式由证据 md 承载。 | 已完成 by quality(manager) @ 2026-05-09；Q1～Q6 通过；hilog 缺口已在执行与验收中显式降级，不与结论矛盾。 |  |  |
| T-8 | V2交互一致性对版（导入链路文案、状态、入口一致） | ued | [pm, qa] | 1）按计划 §V2-T5 可执行验收口径七条逐项走查并完成差异清单（无关键错配或可接受的显式降级已登记）；2）导入/形态三关键入口不把「建议审阅」与「导入向导」混用入口；3）与 dev 已实现的取消与进度语义无未裁决冲突（有则已进入并行冲突裁决规则并留有纪要或计划脚注） | 待执行 | document/task/requests/plan/[任务经理]2026-05-09-[2026-05-08-MVP-V2]-V2主线八任务派发.md#C-5 |  |  |  |  |  |
| T-9 | V2测试设计与执行（主链、取消链路、异常态最小集） | qa | [pm] | 1）输出可执行测试范围与计划，并 V2-D1～V2-D4（人物/故事/导入/B2）均有对应用例命中与一轮执行记录；2）阻塞/严重缺陷单列状态与闭环或升级结论；3）可追溯证据可按 document/plan/V2/qa/[QA]2026-05-09-V2关键验收证据最小模板.md 最小字段组织（路径/日志/判定） | 待执行 | document/task/requests/plan/[任务经理]2026-05-09-[2026-05-08-MVP-V2]-V2主线八任务派发.md#C-6 |  |  |  |  |  |
| T-10 | C9 API12真机验证与证据归档（取消语义） | qa | [pm] | 1）API12 真机完成 R-05/C9 方案要求的最小场景，结论为通过或有条件通过并列明条件时限；2）取证（hilog/等价输出、构建号、机型）归入 document/evidence/V2/…，满足 V2-D5 与模板主档；3）与同版 B2 结合场景已覆盖或明示缺口与风险（不得以拆版绕过 P-3） | 待执行 | document/task/requests/plan/[任务经理]2026-05-09-[2026-05-08-MVP-V2]-V2主线八任务派发.md#C-7 |  |  |  |  |  |
| T-11 | V2版本结项与V3输入边界归档（含风险与暂缓项） | pm | [qa] | 1）《结项说明》补齐 V2-D7/V2-D8：未闭项、风险责任、暂缓项与本版本 V2 对齐；2）V3 准入输入边界写清且无与 P-1 隐含冲突（若扩范围须 CR-x）；3）pm/qa 会签可追溯，关键结论有路径锚点指向本仓库文档 | 待执行 | document/task/requests/plan/[任务经理]2026-05-09-[2026-05-08-MVP-V2]-V2主线八任务派发.md#C-8 |  |  |  |  |  |

## §待入站

| request_id | 请求文件 | item_count | status |
|------------|----------|------------|--------|
| TR-20260509-02 | document/task/requests/plan/[任务经理]2026-05-09-[2026-05-08-MVP-V2]-V2主线八任务派发.md | 8 | accepted |
| TR-20260509-01 | document/task/requests/review/[任务经理]2026-05-09-[2026-05-08-A-1-A-2-A-4]-V2前置修订打包.md | 3 | closed |

## §归档索引

| 月份 | 归档文件 | 说明 |
|------|----------|------|
| 2026-05 | `document/task/archive/2026-05-09/[任务经理]T-1-V2并行冲突裁决.md` | T-1 · TR-20260509-01#C-1 · 2026-05-09 |
| 2026-05 | `document/task/archive/2026-05-09/[任务经理]T-2-V2关键验收证据最小模板.md` | T-2 · TR-20260509-01#C-2 · 2026-05-09 |
| 2026-05 | `document/task/archive/2026-05-09/[任务经理]T-3-V2-T5验收口径固化.md` | T-3 · TR-20260509-01#C-3 · 2026-05-09 |

## §阻塞单

| 编号 | 关联条目 | 阻塞类型 | 状态 |
|------|----------|----------|------|
| - | - | - | 暂无 |
