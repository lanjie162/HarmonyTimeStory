# 最终工作计划：分层 TDD 测试能力落地

> 上游依据：[QA]测试能力升级方案_分层TDD架构.md、2026-05-30-TDD架构方案评审决策纪要.md
> 策略选择：**策略 A（先基建后业务）**
> 决策确认：P-1-A（按依赖顺序）、P-2-A（时间戳后缀独立库）
> 定稿日期：2026-05-30

---

## 一、总体策略

- **目标**：建立分层 TDD 测试架构，补齐测试空白，防止代码变更有安全网
- **策略**：先基建后业务（策略 A），按依赖顺序推进
- **数据库方案**：TestDbHelper 使用时间戳后缀独立数据库文件，不依赖 HarmonyOS RDB 内存模式

---

## 二、梯度划分

| 梯度 | 包含阶段 | 优先级 | 验收强度 |
|------|---------|--------|---------|
| **V1.0** | 阶段1（基建）+ 阶段2（Service L3 测试）+ 阶段5（规范文档） | **P0** | L2（真机证据） |
| **V1.1** | 阶段3（页面逻辑 L2 测试）+ 阶段4（E2E 冒烟测试） | P1 | L2（关键路径真机验证） |
| **V1.2** | 阶段6（CI 自动化）+ 阶段5（TDD 工作流收尾） | P2 | L1（本地验证） |

---

## 三、任务列表

### V1.0（P0 · 最高优先级）

| 任务 ID | 任务名称 | DoD（验收标准） | 依赖关系 |
|---------|---------|----------------|---------|
| T-51 | TestDataFactory 实现 | 可调用 createPerson/createStory/createPhoto 工厂方法，支持参数化造数 | 无 |
| T-52 | TestDbHelper 实现（时间戳独立库） | 支持独立测试库创建、清理、每次运行使用唯一文件名 | 无 |
| T-53 | ServiceTestBase 基类实现 | setup/teardown 封装，Service 实例化，数据库隔离 | T-51 + T-52 |
| T-54 | PersonService L3 集成测试 | 所有公开方法有测试覆盖（~10 方法，~25 条用例） | T-53 |
| T-55 | StoryService L3 集成测试 | 所有公开方法有测试覆盖（~10 方法，~25 条用例） | T-53 |
| T-56 | LinkService + PhotoService L3 集成测试 | 关键方法有测试覆盖（~14 方法，~35 条用例） | T-53 |
| T-57 | 近期 6 个 bug 回归用例 | 6 个已知 bug 的测试用例已加入对应测试文件 | T-54 + T-55 + T-56 |
| T-58 | project_rules.md 规范文档补充 | 分层 TDD 规范已写入 project_rules.md | 无 |

### V1.1（P1 · 中优先级）

| 任务 ID | 任务名称 | DoD（验收标准） | 依赖关系 |
|---------|---------|----------------|---------|
| T-59 | PersonDetailPage 逻辑提取 + L2 测试 | buildMonthGroups/formatBirthday/typeLabel 等提取为纯函数并测试 | V1.0 完成 |
| T-60 | StoryDetailPage 逻辑提取 + L2 测试 | buildDateRangeText/buildMetaInfoText/getDisplayTags 等提取并测试 | V1.0 完成 |
| T-61 | PersonDetailPage E2E 冒烟测试 | 打开页面→显示信息→时间线分组→PhotoSwiper→编辑跳转可跑通 | T-59 |
| T-62 | StoryDetailPage E2E 冒烟测试 | 打开页面→显示信息→照片网格→关联人物可见可跑通 | T-60 |
| T-63 | 编辑页 E2E 冒烟测试 | 修改名称/标题→保存→返回→已更新可跑通 | T-59 + T-60 |

### V1.2（P2 · 低优先级）

| 任务 ID | 任务名称 | DoD（验收标准） | 依赖关系 |
|---------|---------|----------------|---------|
| T-64 | 测试脚本与运行指南 | hdc aa test 可一键运行所有测试，输出结果可读 | V1.0 + V1.1 |
| T-65 | 完整 TDD 工作流规范文档 | PR 模板、测试先行检查清单、Red/Green/Refactor 流程已定义 | 无 |
| T-66 | CI 自动化与覆盖率脚本 | 静态检查门禁（Tier0）+ 手动触发全量测试 | T-64 |

---

## 四、风险与假设

| 风险项 | 等级 | 缓解措施 |
|--------|------|---------|
| TestDbHelper 独立库实现复杂度 | 中 | 参照现有 Tier1RdbIntegration.test.ets 的做法，每次测试创建独立 `.db` 文件 |
| Service 测试覆盖率不够 | 高 | 近期 6 个 bug 的回归用例优先覆盖，后续逐步补齐 |
| 页面逻辑提取导致行为变化 | 中 | 提取前后双验证，先加安全网再重构（QA 验证） |

---

## 五、排期建议

| 梯度 | 执行顺序 | 建议窗口 |
|------|---------|---------|
| V1.0 | 第 1 批 | 立即执行，按 T-50 → T-51 → T-52 → T-53 → T-54 → T-55 → T-56 → T-57 顺序 |
| V1.1 | V1.0 完成后 | 第二批 |
| V1.2 | V1.0 + V1.1 完成后 | 第三批 |

---

## 六、定稿记录

- 策略决策：S-1 → 策略 A（先基建后业务）
- 优先级决策：P-1 → P-1-A（按依赖顺序）
- 数据库决策：P-2 → P-2-A（时间戳后缀独立库）
- 定稿确认：2026-05-30 用户确认定稿