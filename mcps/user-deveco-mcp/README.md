# user-deveco-mcp 工具描述符（仓库镜像）

本目录为 Cursor **user-deveco-mcp** 工具 JSON 的**只读镜像**，便于在无法打开 MCP 面板时仍可按 [document/techdoc/[架构]HarmonyOS测试分层与自动化规范.md](../../document/techdoc/[架构]HarmonyOS测试分层与自动化规范.md) §2.1 核对参数。

- 权威运行环境仍以本机 Cursor 已连接的 MCP 为准；若上游工具参数变更，请同步更新本目录下对应 `.json`。
- 调用前须阅读具体工具的 `arguments` / `required` 字段（与 `qa` 子代理纪律一致）。
