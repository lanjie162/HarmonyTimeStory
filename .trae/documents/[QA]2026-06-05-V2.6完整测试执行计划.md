# 时光故事 MVP V2.6 完整测试执行计划

**角色**：qa（测试工程师）
**日期**：2026-06-05
**版本基线**：V2.6（封板版）
**前置依赖**：T-51~T-58 已编码提交（构建通过），L2 本地测试已就绪

---

## 一、当前状态总览

| 阶段 | 内容 | 状态 | 用例数 | 阻塞项 |
|------|------|------|--------|--------|
| **Phase 1** | L3 集成测试真机执行 | ⏳ 阻塞 | 79条 + 6 bug回归 | **无设备连接** |
| **Phase 2** | L2 页面逻辑测试 | ✅ 已完成 | 39条（L2Person 20 + L2Story 19） | — |
| **Phase 3** | L4 核心UI回归（编码+执行） | ❌ 未开始 | ~19条待编码 | — |
| **Phase 4** | V2.6 封板功能测试 | ❌ 未开始 | ~23条待编码 | — |
| **Phase 5** | 补齐与加固 | ❌ 未开始 | ~20条待编码 | — |

### 已存在可运行的测试（60+条）

| 测试文件 | 层级 | 用例数 | 运行位置 |
|----------|------|--------|----------|
| L2PersonDetailLogic.test.ets | L2 | 20 | src/test/（本地） |
| L2StoryDetailLogic.test.ets | L2 | 19 | src/test/（本地） |
| LocalUnit.test.ets | L1 | 7 | src/test/（本地） |
| V2RegressionPerson.test.ets | L4 | ~15 | ohosTest/（真机） |
| V2RegressionStory.test.ets | L4 | ~12 | ohosTest/（真机） |
| V2RegressionSuggestImport.test.ets | L4 | ~6 | ohosTest/（真机） |
| V2RegressionImportFull.test.ets | L4 | ~6 | ohosTest/（真机） |
| V2RegressionB2.test.ets | L4 | ~3 | ohosTest/（真机） |
| V2RegressionShell.test.ets | L4 | ~3 | ohosTest/（真机） |
| Tier1RdbIntegration.test.ets | L3 | ~10 | ohosTest/（真机） |

---

## 二、执行策略调整（基于实际阻塞项）

### 2.1 核心阻塞：Phase 1 L3 真机执行无设备

**影响范围**：
- 79条 L3 集成测试无法真机执行
- 6个已知 bug 回归验证无法在真机确认
- T-51~T-56 的 L2 级证据（hdc aa test 日志）无法交付

**缓解方案**：
1. **L2 本地测试先行**（Phase 2 已完成，可直接本地跑）
2. **L4 E2E 优先编码**（Phase 3 + Phase 4），不依赖真机执行即可编写代码
3. **Phase 1 降级为"设备就绪后补执行"**，不影响 Phase 3~5 的编码工作
4. Q-1-1~Q-1-4 的 DoD 证据等级从 L2 暂标为 **L1（代码已提交+构建通过）**，待设备就绪后补 L2 证据

### 2.2 执行顺序（现实可行）

```
Step 1: 本地 L2 测试执行 ✅（今日可做）
  └── L2PersonDetailLogic.test.ets 20条 + L2StoryDetailLogic.test.ets 19条

Step 2: Phase 3 L4 核心UI回归编码（今日起）
  └── 创建 V2RegressionTabs / PhotoSwiper / BottomSheet / SwipeAction 测试文件

Step 3: Phase 4 V2.6 封板功能编码
  └── ImportService L3 测试 + 条件页/形态一/ShellPage/隐私页 E2E

Step 4: 设备就绪 → 集中真机执行
  └── Phase 1 L3 79条 + 全部 L4 E2E ~50条
```

---

## 三、Phase 2 本地 L2 测试执行

### 3.1 执行方式

```bash
# entry@default 构建已验证通过
hvigor assembleDefault

# entry@ohosTest 构建已验证通过
hvigor assembleOhosTest
```

### 3.2 已覆盖函数清单

| 测试文件 | 函数 | 用例数 | 覆盖场景 |
|----------|------|--------|----------|
| L2PersonDetailLogic | `parsePersonId` | 4 | 正常ID、NaN、负数、浮点数 |
| L2PersonDetailLogic | `typeLabel` | 4 | BABY/ADULT/ELDER/OTHER |
| L2PersonDetailLogic | `genderLabel` | 4 | MALE/FEMALE/UNSPECIFIED/null |
| L2PersonDetailLogic | `formatBirthday` | 4 | 有效日期、null、0、边缘日期 |
| L2PersonDetailLogic | `formatMonthLabel` | 2 | 正常月份、边缘值 |
| L2PersonDetailLogic | `buildMonthGroups` | 7 | 空数据/单照片/单故事/skip无时间/同月混排/多月份降序/coverUri匹配 |
| L2PersonDetailLogic | `blobToBase64` | 2 | 空blob/null |
| L2StoryDetailLogic | `parseStoryId` | 4 | 正常ID、NaN、负数、浮点数 |
| L2StoryDetailLogic | `formatDate` | 4 | 有效日期、null、0、边缘日期 |
| L2StoryDetailLogic | `buildDateRangeText` | 4 | 起止完整、起为止null、止为null、均为null |
| L2StoryDetailLogic | `buildMetaInfoText` | 5 | 全字段/部分字段/空字段/长文本截断/特殊字符 |
| L2StoryDetailLogic | `getDisplayTags` | 6 | 0-5个标签 + 空数组 |
| L2StoryDetailLogic | `getExtraTagCount` | 5 | 0-5个标签 + 边界 |

### 3.3 Bug回归覆盖

- **Bug #1**：`buildMonthGroups` 返回空分组 → L2PersonDetailLogic 覆盖
- **Bug #5**：`formatBirthday` 空值映射 → L2PersonDetailLogic 覆盖

---

## 四、Phase 3 L4 核心UI回归（待编码）

### 4.1 V2RegressionTabs.test.ets（~5条）

| 用例 | 场景 | 关键验证点 |
|------|------|-----------|
| Tabs_switch_person_story | 人物Tab→故事Tab切换 | 故事列表可见 |
| Tabs_switch_story_person | 故事Tab→人物Tab切换 | 人物列表可见 |
| Tabs_gesture_swipe | 手势滑动切换Tab | Tab内容更新 |
| Tabs_router_back | router.back 回到正确Tab | 回到ShellPage后Tab状态保持 |
| Tabs_maintain_state | Tab状态保持（不重建） | 切换前后列表内容一致 |

### 4.2 V2RegressionPhotoSwiper.test.ets（~6条）

| 用例 | 场景 | 关键验证点 |
|------|------|-----------|
| PhotoSwiper_open_from_grid | 点击照片网格→全屏 | 黑色背景Swiper出现 |
| PhotoSwiper_swipe_left_right | 左右滑动切换照片 | 照片内容更新 |
| PhotoSwiper_swipe_down_close | 下滑关闭 | 回到详情页 |
| PhotoSwiper_menu_open | 点击⋮菜单 | 菜单选项可见 |
| PhotoSwiper_menu_remove | 菜单→移除照片 | 照片从网格消失 |
| PhotoSwiper_broken_uri | URI失效态显示 | 灰底+失效文案可见 |

### 4.3 V2RegressionBottomSheet.test.ets（~4条）

| 用例 | 场景 | 关键验证点 |
|------|------|-----------|
| BottomSheet_open_person | 人物页[+创建] | BottomSheet弹出 |
| BottomSheet_create_person | 输入名称→创建 | 列表刷新，新人物可见 |
| BottomSheet_open_story | 故事页[+创建] | BottomSheet弹出 |
| BottomSheet_create_story | 输入标题→创建 | 列表刷新，新故事可见 |

### 4.4 V2RegressionSwipeAction.test.ets（~4条）

| 用例 | 场景 | 关键验证点 |
|------|------|-----------|
| SwipeAction_person_delete | 人物行左滑→确认删除 | 人物从列表消失 |
| SwipeAction_person_cancel | 人物行左滑→取消删除 | 人物仍在列表 |
| SwipeAction_story_delete | 故事行左滑→确认删除 | 故事从列表消失 |
| SwipeAction_story_cancel | 故事行左滑→取消删除 | 故事仍在列表 |

---

## 五、Phase 4 V2.6 封板功能测试（待编码）

### 5.1 ImportService L3 集成测试（~10条）

| 用例 | 场景 | 关键验证点 |
|------|------|-----------|
| importService_time_filter | 时间范围过滤 | 仅返回时间段内照片 |
| importService_single_day | 单日过滤（timeStart===timeEnd） | 正确筛选 |
| importService_location_filter | 地点过滤 | 仅匹配地点 |
| importService_and_combination | 时间+地点AND组合 | 交集结果正确 |
| importService_empty_result | 无匹配结果 | 返回空列表 |
| importService_full_flow | 形态一完整流程 | 扫描→候选→写入StoryPhoto |

### 5.2 导入条件页 E2E（~5条）

| 用例 | 场景 | 关键验证点 |
|------|------|-----------|
| ImportCondition_date_picker | 时间Picker选择起止 | 条件显示正确 |
| ImportCondition_location_input | 地点输入 | 地点条件生效 |
| ImportCondition_and_combination | 时间+地点组合 | AND筛选生效 |
| ImportCondition_reset | 重置条件 | 恢复默认 |
| ImportCondition_back | 返回上一步 | 回到导入准备步 |

### 5.3 形态一完整流程 E2E（~3条）

| 用例 | 场景 | 关键验证点 |
|------|------|-----------|
| Form1_full_flow_person | 人物→导入→写入 | StoryPhoto 写入成功 |
| Form1_full_flow_story | 故事→导入→写入 | StoryPhoto 写入成功 |
| Form1_cancel_midway | 中途取消 | 回到详情页，数据一致 |

### 5.4 ShellPage 2Tab + 空态 E2E（~3条）

| 用例 | 场景 | 关键验证点 |
|------|------|-----------|
| ShellPage_two_tabs | 2 Tab（人物+故事） | 无建议Tab |
| SuggestPage_empty | SuggestPage路由保留 | 空态文案"暂无建议" |
| SuggestPage_routing | SuggestPage导航 | 可进入/返回 |

### 5.5 隐私页人脸移除（~2条）

| 用例 | 场景 | 关键验证点 |
|------|------|-----------|
| PrivacyPage_no_face_block | 隐私页无人脸文本块 | 6板块保留，无人脸 |
| SettingsPage_no_face_entry | 设置页无人脸入口 | 无"人脸开关"和"缓存清除" |

---

## 六、Phase 5 补齐与加固（待编码，低优先级）

### 6.1 L1 domain/model 测试（~5条）

- `GenderEnum` 枚举值完整性
- `PersonTypeEnum` 枚举值完整性
- `PersonModel` 接口字段类型检查
- `StoryModel` 接口字段类型检查
- `PhotoMetadata` 可选字段检查

### 6.2 L2 补充（~5条）

- `StoryDetailLogic` 剩余边界补充
- PhotoSwiper 逻辑函数提取+测试
- EditPages 逻辑函数提取+测试

### 6.3 L4 补充（~10条）

- 删除级联 E2E（Person删除→关联Story不变→PhotoRef保留）
- GC E2E（启动后孤儿PhotoRef清理）
- 上限阻断 E2E（1001张对话框）
- 失效态 E2E（LOCAL_DELETED/CLOUD_INACCESSIBLE）
- 权限拒绝 E2E（引导文字+系统设置跳转）
- 崩溃上报 E2E（FaultLogger集成）

---

## 七、执行时间线（建议）

| 步骤 | 内容 | 前置依赖 | 预估时间 |
|------|------|----------|----------|
| **S-1** | Phase 2 L2 本地执行 + 结果确认 | 无 | 0.5天 |
| **S-2** | Phase 3 L4 核心UI测试编码 | 无 | 1天 |
| **S-3** | Phase 4 V2.6封板功能测试编码 | S-2 完成 | 1.5天 |
| **S-4** | 设备就绪 → 全量真机执行（Phase 1+3+4） | 设备连接 | 2天 |
| **S-5** | Phase 5 补齐加固 | S-4 完成 | 2天 |
| **S-6** | 缺陷修复+回归+发布裁决 | S-4~S-5 结果 | 1天 |
| **合计** | | | **8天（含设备等待）** |

---

## 八、风险与缓解

| 风险 | 影响 | 概率 | 缓解 |
|------|------|------|------|
| **Phase 1 L3 真机持续无设备** | 79条+6bug回归无法交付L2证据 | 高 | 降级为L1（代码+构建通过），标注"待设备就绪补L2" |
| **既有E2E因UI重构失效** | Phase 3~4 编码后发现基线不对 | 中 | 先单独构建ohosTest验证既有E2E，确认基线后再编码新用例 |
| **ImportService L3 测试需要真机RDB** | Phase 4.1 不可本地执行 | 高 | 先编写测试代码，真机就绪后统一执行 |
| **V2.6 功能未完全合并到main** | 新E2E无法通过 | 中 | 确认T-37a/T-60/T-61合并状态后再编码对应的E2E |

---

## 九、验收标准（QA视角）

1. **L2 本地测试** ≥39条全部 PASS（今日可验证）
2. **L4 核心UI回归** ≥19条，覆盖 Tabs/PhotoSwiper/BottomSheet/SwipeAction
3. **V2.6 封板功能** ≥23条，覆盖 ImportService L3/条件页E2E/形态一/ShellPage/隐私页
4. **补齐加固** ≥20条，覆盖 L1 domain/model/L2补充/L4补充
5. **真机执行**：全部 L3+L4 测试在设备就绪后集中执行，交付 L2 证据
6. **BUG回归**：6个已知bug回归用例全部 PASS
7. **发布裁决**：无 P0 阻塞缺陷残留，输出 V2.6 封板版通过/有条件通过/不通过结论

---

## 十、下一步建议

1. **优先执行 Phase 2 L2 本地测试**（今日可做，确认 L2PersonDetailLogic 和 L2StoryDetailLogic 全部 PASS）
2. **并行启动 Phase 3 编码**（不依赖设备）
3. **告知设备状态**：需要用户确认是否可连接真机/模拟器，以解封 Phase 1
4. **确认 V2.6 功能合并状态**：T-37a/T-60/T-61 是否已合并到 main